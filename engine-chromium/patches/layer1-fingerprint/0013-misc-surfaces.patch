# Rationale: Remaining surfaces: getClientRects/getBoundingClientRect optional subpixel jitter; performance.now/timeOrigin resolution clamped to the claimed build's real behavior; Battery/Gamepad/Sensors/NetworkInformation persona-appropriate availability (docs/tdd/01 §4.12).
# Surface: clientRects, performance.now, battery, gamepad, sensors
# Threat-vectors: V1, V2, V3
# Upstream-risk: MED. Scattered across several modules; individually small.
# Tests: v3-traces/*, v1-coherence/perf-precision
# Status: PLACEHOLDER (M0). Implemented against a real Chromium tree in M1.
#
# This header is a valid, reviewable specification. The unified diff below is
# empty in M0 (no Chromium checkout to diff against) and will be produced by
# `git format-patch` against the pinned baseline (see ../../CHROMIUM_BASELINE)
# during M1 implementation.
#
# (no diff hunks yet — M0 placeholder)
