# Rationale: Route config.locale.timezone through ICU + V8 Date/Intl so Intl timezone, Date offset, and Accept-Language all agree AND match the proxy IP geolocation (docs/tdd/01 §4.9). Ties to the network layer (tdd/03).
# Surface: Timezone / locale
# Threat-vectors: V1 (tz-geo, lang)
# Upstream-risk: MED. ICU tz plumbing + V8 Date/Intl bridge + Accept-Language header path.
# Tests: v1-coherence/tz-geo, v1-coherence/lang
# Target-milestone: M1
# Status: BACKLOG
#
# This file is a valid, reviewable M1 backlog specification. Its git-format
# patch payload will be produced against the pinned baseline (see
# ../../CHROMIUM_BASELINE) during M1 implementation.
#
# (no diff hunks yet — M1 backlog placeholder)
