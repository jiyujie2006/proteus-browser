# Rationale: Make navigator.{userAgent,platform,languages,vendor,hardwareConcurrency,deviceMemory,appVersion,oscpu} return config values natively (docs/tdd/01 §4.1). Native getters => correct toString/descriptors/prototype for free (ADR 0005).
# Surface: navigator.*
# Threat-vectors: V1 (coherence), V3 (native, no trace)
# Upstream-risk: MED. Touches core/frame/navigator*.cc, navigator_ua_data.cc, navigator_concurrent_hardware.cc, navigator_device_memory.cc, navigator_language.cc. Upstream occasionally reshuffles these.
# Tests: v1-coherence/platform-os, v1-coherence/hw-pair, v3-traces/native-tostring, v3-traces/cross-context
# Target-milestone: M1
# Status: BACKLOG
#
# This file is a valid, reviewable M1 backlog specification. Its git-format
# patch payload will be produced against the pinned baseline (see
# ../../CHROMIUM_BASELINE) during M1 implementation.
#
# (no diff hunks yet — M1 backlog placeholder)
