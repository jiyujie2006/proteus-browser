# Rationale: Derive BOTH navigator.userAgentData AND the outgoing Sec-CH-UA* request headers from the same config values so UA and CH cannot disagree; full-version-list internally consistent with engine.fullVersion (docs/tdd/01 §4.2).
# Surface: Client Hints (JS + HTTP headers)
# Threat-vectors: V1 (ua-ch)
# Upstream-risk: MED. UA-CH plumbing in //content + navigator_ua_data.cc.
# Tests: v1-coherence/ua-ch
# Status: PLACEHOLDER (M0). Implemented against a real Chromium tree in M1.
#
# This header is a valid, reviewable specification. The unified diff below is
# empty in M0 (no Chromium checkout to diff against) and will be produced by
# `git format-patch` against the pinned baseline (see ../../CHROMIUM_BASELINE)
# during M1 implementation.
#
# (no diff hunks yet — M0 placeholder)
