# Rationale: WebGL/WebGL2 vendor/renderer (UNMASKED_*), VERSION, extensions, precision, and readPixels noise from config; GPU must be POSSIBLE on the persona OS (no Apple renderer on Win32) (docs/tdd/01 §4.5).
# Surface: WebGL / WebGL2
# Threat-vectors: V1 (platform-gpu), V2, V3
# Upstream-risk: HIGH. modules/webgl/webgl_rendering_context_base.cc is large and upstream-active; a refactor here is likely on some rebases.
# Tests: v1-coherence/platform-gpu, v1-coherence/webgl-webgpu, v3-traces/native-tostring
# Status: PLACEHOLDER (M0). Implemented against a real Chromium tree in M1.
#
# This header is a valid, reviewable specification. The unified diff below is
# empty in M0 (no Chromium checkout to diff against) and will be produced by
# `git format-patch` against the pinned baseline (see ../../CHROMIUM_BASELINE)
# during M1 implementation.
#
# (no diff hunks yet — M0 placeholder)
