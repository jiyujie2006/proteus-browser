# Rationale: Present the persona OS font set (bundled per-OS packages) and restrict matching (local(), document.fonts, measurement) so only those resolve regardless of host fonts; metrics reflect presented fonts (docs/tdd/01 §4.8). Hardest on Linux hosts.
# Surface: Fonts (enumeration + metrics)
# Threat-vectors: V1 (font-os)
# Upstream-risk: MED. platform/fonts/font_cache*.cc, FontManager, text-metrics paths; plus bundled-font packaging + licensing (see ../../../docs/10-third-party-licensing.md).
# Tests: v1-coherence/font-os
# Target-milestone: M1
# Status: BACKLOG
#
# This file is a valid, reviewable M1 backlog specification. Its git-format
# patch payload will be produced against the pinned baseline (see
# ../../CHROMIUM_BASELINE) during M1 implementation.
#
# (no diff hunks yet — M1 backlog placeholder)
