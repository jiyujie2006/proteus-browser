# Rationale: Seeded, extremely small perturbation on AudioContext/OfflineAudioContext float output (HMAC(seed,'audio')), within natural audio-stack variation; deterministic per profile, consistent across contexts (docs/tdd/01 §4.7).
# Surface: AudioContext
# Threat-vectors: V2 (stable-unique/coarse-common), V3
# Upstream-risk: LOW-MED. modules/webaudio/ readback paths.
# Tests: v3-traces/cross-context, v2-rarity/audio-stability
# Status: PLACEHOLDER (M0). Implemented against a real Chromium tree in M1.
#
# This header is a valid, reviewable specification. The unified diff below is
# empty in M0 (no Chromium checkout to diff against) and will be produced by
# `git format-patch` against the pinned baseline (see ../../CHROMIUM_BASELINE)
# during M1 implementation.
#
# (no diff hunks yet — M0 placeholder)
