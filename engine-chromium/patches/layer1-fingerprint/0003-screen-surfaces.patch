# Rationale: Screen/window surfaces (width/height/avail*/colorDepth/pixelDepth/devicePixelRatio and derived outer/inner relationships) from config; the actual opened window must match reported inner sizes (docs/tdd/01 §4.3).
# Surface: screen / window
# Threat-vectors: V1 (real tuple), V5 (window matches reported size)
# Upstream-risk: MED. core/frame/screen.cc + widget/frame sizing.
# Tests: v1-coherence/screen-real
# Status: PLACEHOLDER (M0). Implemented against a real Chromium tree in M1.
#
# This header is a valid, reviewable specification. The unified diff below is
# empty in M0 (no Chromium checkout to diff against) and will be produced by
# `git format-patch` against the pinned baseline (see ../../CHROMIUM_BASELINE)
# during M1 implementation.
#
# (no diff hunks yet — M0 placeholder)
