# Rationale: Deterministic, per-profile, bounded perturbation on the canvas readback path (toDataURL/toBlob/getImageData), seeded by HMAC(seed,'canvas'). Stable per profile across sessions, different across profiles, amplitude within hw-natural bounds (docs/tdd/01 §4.4).
# Surface: Canvas 2D readback
# Threat-vectors: V2 (stable-unique but coarse-common), V3 (consistent across contexts)
# Upstream-risk: MED. html_canvas_element.cc (ToDataURLInternal, toBlob) + 2D getImageData.
# Tests: v3-traces/cross-context, v2-rarity/canvas-stability
# Target-milestone: M1
# Status: BACKLOG
#
# This file is a valid, reviewable M1 backlog specification. Its git-format
# patch payload will be produced against the pinned baseline (see
# ../../CHROMIUM_BASELINE) during M1 implementation.
#
# (no diff hunks yet — M1 backlog placeholder)
