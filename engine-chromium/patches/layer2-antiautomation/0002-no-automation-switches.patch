# Rationale: Remove the 'controlled by automated test software' infobar and --enable-automation-triggered observable behaviors so enabling remote debugging doesn't flip visible/observable automation state (docs/tdd/04 §2).
# Surface: automation switches / infobar
# Threat-vectors: V5
# Upstream-risk: MED. //chrome/browser automation/infobar wiring.
# Tests: v5-automation/no-infobar, v5-automation/no-switches
# Target-milestone: M1
# Status: BACKLOG
#
# This file is a valid, reviewable M1 backlog specification. Its git-format
# patch payload will be produced against the pinned baseline (see
# ../../CHROMIUM_BASELINE) during M1 implementation.
#
# (no diff hunks yet — M1 backlog placeholder)
