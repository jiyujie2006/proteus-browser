# Rationale: Expose a CDP-compatible endpoint that obtains execution contexts WITHOUT the page-observable Runtime.enable side effects (isolated-world evaluation), exposes no automation identity, and is local-bound + authenticated (docs/tdd/04 §3-4). Robust across framework versions because it's in-engine, not a client shim.
# Surface: stealth CDP endpoint / Runtime.enable leak
# Threat-vectors: V5
# Upstream-risk: HIGH. Touches CDP/DevTools protocol handling and isolated-world evaluation; upstream DevTools changes are frequent. Second-highest-attention item after config-ingest.
# Tests: v5-automation/runtime-enable-leak, v5-automation/no-cdp-artifacts
# Target-milestone: M3
# Status: BACKLOG
#
# This file is a valid, reviewable M3 backlog specification. Its git-format
# patch payload will be produced against the pinned baseline (see
# ../../CHROMIUM_BASELINE) during M3 implementation.
#
# (no diff hunks yet — M3 backlog placeholder)
