# Pinned Chromium baseline that the patch series targets and the tracking bot
# watches. The tracking bot (tracking-bot/pipeline.mjs) compares the latest
# upstream stable against this and, when newer, drives the rebase pipeline.
#
# Format: one KEY=VALUE per line. Keep in sync with build manifests.
CHROMIUM_STABLE=150.0.7871.124
CHANNEL=stable
# Milestone this baseline corresponds to (informational).
MILESTONE=150
# Date the baseline was pinned (ISO). Updated by the tracking bot on promotion.
PINNED_AT=2026-07-17
